# RDoc  -*- sh -*-

rdoc_check() {
  ${SUDO_GEM} gem list rdoc | grep rdoc >/dev/null
}

rdoc_install() {
  if rdoc_check ; then
    echo "RDoc already installed"
  else
    ${SUDO_GEM} gem install rdoc --no-rdoc --no-ri
  fi
}

rdoc_uninstall() {
  ${SUDO_GEM} gem uninstall -a rdoc
}
