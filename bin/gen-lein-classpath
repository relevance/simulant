#!/bin/sh
#
# Generate lein classpath in a file system agnostic way
#

SVC_ROOT=$(cd  $(dirname $0)/..; pwd)
export SVC_ROOT

usage() {
  cat <<EOF
USAGE:

  ${me} <output-file>

$1

EOF

}
lein classpath | sed "s/$(echo ${SVC_ROOT} | sed 's/\//\\\//g')/$\{SVC_ROOT\}/g" > $1
